#
#
# Extension Target

extension_list = [  
  'sms',
  'filemanager-python',
  'generic'
]

foreach module : extension_list
  subdir(module)
endforeach



#install_data(['helper.py', '__init__.py'], install_dir: plugin_subdir)
