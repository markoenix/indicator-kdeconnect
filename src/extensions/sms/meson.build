# Executable type

install_data('sms.py', 
             install_dir: get_option('bindir'))  

#run_command('chmod', '+x', join_paths(get_option('bindir'), 'sms.py'))                                     